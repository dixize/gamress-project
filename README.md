# Скопируй этот файл в .env.local для локальной разработки (vercel dev)
# В самом Vercel эти переменные задаются в Project → Settings → Environment Variables — .env файлы туда не грузятся.

# Обязательно для уведомлений в Telegram:
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Опционально — для сохранения броней в базу (Supabase), см. supabase/schema.sql:
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
